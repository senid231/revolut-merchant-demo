<nav class="bg-white border-gray-200 px-2 sm:px-4 py-2.5 rounded">
  <div class="flex flex-wrap items-center justify-between w-full">
    <a href="/" class="flex items-center">
      <span class="self-center text-xl font-semibold whitespace-nowrap">Revolut Merchant</span>
    </a>

    <div class="block w-auto">
      <ul class="flex flex-col p-4 mt-4 border border-gray-100 rounded-lg bg-gray-50 md:flex-row md:space-x-8 md:mt-0 md:text-sm md:font-medium md:border-0 md:bg-white">
        <li>
          <%= link_to 'Customers',
                      customers_path,
                      class: class_names(
                        'navbar-menu-item-active': current_menu == :customers,
                        'navbar-menu-item-inactive': current_menu != :customers
                      ) %>
        </li>
        <li>
          <%= link_to 'Payment Methods',
                      payment_methods_path,
                      class: class_names(
                        'navbar-menu-item-active': current_menu == :payment_methods,
                        'navbar-menu-item-inactive': current_menu != :payment_methods
                      ) %>
        </li>
        <li>
          <%= link_to 'Payments',
                      payments_path,
                      class: class_names(
                        'navbar-menu-item-active': current_menu == :payments,
                        'navbar-menu-item-inactive': current_menu != :payments
                      ) %>
        </li>
        <li>
          <%= link_to 'Payment Transactions',
                      payment_transactions_path,
                      class: class_names(
                        'navbar-menu-item-active': current_menu == :payment_transactions,
                        'navbar-menu-item-inactive': current_menu != :payment_transactions
                      ) %>
        </li>
      </ul>
    </div>
  </div>
</nav>
