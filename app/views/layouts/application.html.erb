<!DOCTYPE html>
<html>
<head>
  <title>RevolutMerchant</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= render 'layouts/revolut_checkout' %>
  <%= javascript_importmap_tags %>
</head>

<body class="bg-[#f5f5f5]" data-controller="modal">
<%= render partial: 'layouts/flashes' %>

<div class="flex flex-col grow min-h-screen">
  <%= render partial: 'layouts/navbar' %>

  <main class="relative grow px-4 pt-8 pb-12 flex justify-center md:min-w-5xl">
    <div>
      <%= yield %>
    </div>
  </main>
</div>

</body>
</html>
